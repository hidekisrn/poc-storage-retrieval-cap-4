server {
    listen       80;
    server_name  _;

    root   /usr/share/nginx/html;
    index  index.html;

    # Garante que HTML/CSS/JS sejam servidos como UTF-8
    charset        utf-8;
    source_charset utf-8;

    location / {
        try_files $uri $uri/ =404;
    }
}
